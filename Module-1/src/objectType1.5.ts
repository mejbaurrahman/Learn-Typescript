/* 
Reference Type --- object
*/

const user:{
    company: 'Google', // type will be Google (literal type (value will be altime same then the value itself a type named literal types))
    firstName: string,
    middleName?: string, // optional
    lastName: string, 
    isMarried?: boolean
} = {
    company:'Google',
    firstName: 'Mejba',
    middleName: '',
    lastName: 'Rahman',
    isMarried: true
}
